apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nodejs-api.fullname" . }}-config
  namespace: {{ .Values.namespace }}
data:
  LOG_LEVEL: "info"
  # Thêm config khác ở đây (không chứa secret — secret phải qua External Secrets)
